export function Welcome({ title }: { title: string }) {
  return (
    <div>
      <h1>Welcome {title}!</h1>
      <div />
      <p>
        To get started, edit <code>src/app/welcome.tsx</code> and save to reload.
      </p>
    </div>
  );
}
export default Welcome;